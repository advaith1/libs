---
const voiceVersionColors = {
	'-': 'gray',
	1: 'red',
	2: 'red',
	3: 'red',
	4: 'red',
	5: 'red',
	6: 'red',
	7: 'red',
	8: 'red',
	E2EE: 'green'
}

interface Props {
	version: number | string
}
const { version } = Astro.props
---

<td
	class={voiceVersionColors[version] ??
		(typeof version === 'string' ? voiceVersionColors[version.split(' ')[0]!] : null) ??
		'red'}
>
	{version}
</td>
